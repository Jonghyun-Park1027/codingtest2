data = list(input())
cursor = [i for i in range(len(data)+1)]
idx = 0
# if cursor[idx] == 0: # L,B 무시의 경우
    # pass
# if cursor[idx] == len(data): # D 무시의 경우
    # pass

a = [0]
while a :
    a = input()
    print("0")